@using Faker.Model
@inherits BaseFieldComponent


@if (Field != null)
{
    <FieldControl Field="Field" RemoveRequested="RemoveField"></FieldControl>
    @if (RandomNumberField != null)
    {
        <MatNumericUpDownField Label="Min" TValue="int"
                       @bind-Value=RandomNumberField.Min>

        </MatNumericUpDownField>
        <MatNumericUpDownField Label="Min" TValue="int"
                               @bind-Value=RandomNumberField.Min>
        </MatNumericUpDownField>
       

    }
}

@code {

    private RandomNumberField? RandomNumberField => Field?.Field as RandomNumberField;


}